/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "radiojar-lib.appspot.com",
      "api.radyoklasik.online",
      "cors-anywhere.herokuapp.com",
    ],
  },
};

export default nextConfig;
